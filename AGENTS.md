# このリポジトリでの作業

ここはアイデアを要求・代案比較から実装引き継ぎまで育て、進め方自体も改善する場所。日本語で書く。

## 依頼の入口

- アイデアの投入、検討の継続、実装前まで練る依頼：`skills/grow-idea/SKILL.md` を読んで実行する。
- アイデア成果物のレビューだけの依頼：`skills/review-idea/SKILL.md` を読む。レビュー依頼だけでは対象設計を書き換えない。
- この場所のフロー・skill の改善：`skills/improve-rail/SKILL.md` を読む。
- 単純な誤字修正や質問には必要な範囲だけ対応する。

パスはリポジトリルート基準。skill が一覧に出なくても上記のファイルを直接読む。外部 plugin がなくても完結する。

## 守る前提

ユーザーが既に伝えた背景・制約・合意を読み直して使い、同じ確認を繰り返さない。調査や比較で決められることをユーザーへ丸投げしない。依頼の範囲内で判断・反映・検証を進め切る。

原文、外部で確認した事実、推論、仮置き、ユーザーの決定を混同しない。未回答は承認ではない。未決事項に依存しない作業を先に終え、依存部分は条件付き案と影響範囲を残す。

標準担当は sol。terra に任せる場合は入力・変更範囲・期待結果・検証・止まる条件を揃える。astra への相談時は矛盾する前提、試した案、反証、求める判断を一つの資料にまとめる。モデル名はユーザー指定の役割として扱い、切替や別エージェント起動を実施したと偽らない。利用できなければ現在の担当が可能な範囲を続ける。

tumeda-dev は任意の参考資料。ここでのアイデア育成に、その工程・承認ゲート・作業ディレクトリを自動追加しない。実装先で同 plugin を使う場合は、実装先の現行契約を確認して引き渡す。

## ファイルの扱い

進め方の正本は `playbook/workflow.md`。個別の採用内容は各アイデアの `proposal.md`、判断履歴は `decisions.md`、根拠は `evidence.md`、状態と未決事項は `README.md` に置く。重複コピーを増やさず相互参照する。

実案件は `ideas/`、架空例とレールの評価は `evaluations/`。秘密情報、個人情報を含む原文は保存前に最小化し、伏せたことを記録する。
アイデア育成の依頼だけでは、製品実装、外部送信、購入、契約、公開を行わない。必要なら手順まで準備し、権限が必要な部分だけ残す。
